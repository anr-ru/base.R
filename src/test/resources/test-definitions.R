# Definitions of input parameters that are used in models.
# 
# Author: 
#
###############################################################################

# A global variables
pp <<- c(1,2,3,4,5);

globb <<- c(1,2);

sx <<- c('1', '2', '3')
sxx <<- c("1", "2", "3")


names(pp) <- c("time.unit", "length.days", "remit.hours",  "depMin", "minTran")

stored <<- 23;
